%! Package = PACKAGE_NAME
%! Author = maad
%! Date = 01.01.2024

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{packages}[2022/11/27 Packages]
\RequirePackage[utf8]{inputenc}

%! Packages
% Appendix
\RequirePackage{appendix}
% Used for commands
\RequirePackage{datatool}
%
\RequirePackage{type1cm}
% Allows index generation
\RequirePackage{makeidx}
% Standard LaTeX graphics tool when including figure files
\RequirePackage{graphicx}
% Used for the two-column index
\RequirePackage{multicol}
% Places footnotes at page bottom
\RequirePackage[bottom]{footmisc}
% Enables controlling the look and feel of captions
\RequirePackage{caption}
% Recommended when making sub-figures
\RequirePackage{subcaption}
% Includes Bibliography, Index, list of Listing etc. to table of contents
\RequirePackage[nottoc]{tocbibind}
% Enable importing of sections
\RequirePackage{import}
% Provides international handling of quote marks. Especially useful for bibliography management using BibLaTeX
\RequirePackage{csquotes}
% Hyper-references, possible to change color
\RequirePackage{hyperref}
\hypersetup{    % Color of hyper-references
    colorlinks,
    citecolor = black,
    filecolor = black,
    linkcolor = black,
    urlcolor = black
}
% Comment blocks of text using \begin{comment} ... \end{comment}
\RequirePackage{comment}
% Enables import of pdf-pages using e.g. \includepdf[pages=-]{./my_pdf.pdf}
\RequirePackage{pdfpages}
% Wrap text around images
\RequirePackage{wrapfig}
% Force image location using "H"
\RequirePackage{float}
% Insert urls
\RequirePackage{url}
% Set url-style as "sans-serif". Other options are e.g. "same" or "rm"
\urlstyle{sf}
% Enable SI units
\RequirePackage{siunitx}
% Defining UK English as language.
% This will among other things ensure that dates are displayed as 24/03/1997 rather than 03/24/1997 in the bibliography.
\RequirePackage[british]{babel}
% Includes several programming languages and styles
%\RequirePackage{minted}
% Bold text in math mode
\RequirePackage{bm}
% Math formulas and improved typographical quality of their output
\RequirePackage{amsmath}
% Extended symbol collection
\RequirePackage{amssymb}
% Helps define theorem-like structures
\RequirePackage{amsthm}
% Used in the package "gensymb" (below), which will give warnings if "textcomp" is not imported in advance
\RequirePackage{textcomp}
% Adds extra generic symbols for math and text mode, e.g. \degree
\RequirePackage{gensymb}
% Page setup
\RequirePackage[a4paper, total={150mm, 245mm,footskip = 14mm}]{geometry}
\setlength{\parindent}{0em}
\setlength{\parskip}{0.8em}
% Customized header and footer
\RequirePackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.1ex}
\renewcommand{\footrulewidth}{0.1ex}
\fancyfoot[C]{\thepage}


%! Commands
\input{commands}




